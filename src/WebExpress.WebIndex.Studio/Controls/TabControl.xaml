<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WebExpress.WebIndex.Studio.Controls.TabControl">
    
    <Grid x:Name="TabsGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!--
        <ItemsControl Grid.Row="0" ItemsSource="{Binding TabItems}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Text="{Binding Name}" Clicked="OnTabClicked"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackLayout Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>-->

        <BoxView x:Name="ActiveTabUnderline" HeightRequest="2" BackgroundColor="Black" VerticalOptions="End"/>

        <ContentView Grid.Row="1" x:Name="ContentArea">

        </ContentView>
    </Grid>
    
</ContentView>
